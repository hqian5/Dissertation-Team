<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dissertation.airline.mapper.BookingMapper">


    <select id="getBookingByPassengerId"  resultType="BookingBean">
		select booking_id, passenger_id, flight_id, seat_id,
        payment_id, discount_id, create_time, update_time
        from booking where passenger_id = #{passengerId}
	</select>

    <select id="getBookingById"  resultType="BookingBean">
        select booking_id, passenger_id, flight_id, seat_id,
        payment_id, discount_id, create_time, update_time
        from booking where booking_id = #{bookingId}
	</select>

    <insert id="addBooking" parameterType="BookingBean" useGeneratedKeys="true" keyProperty="bookingId">
        INSERT INTO booking(passenger_id, flight_id,
        seat_id, payment_id, discount_id) VALUES (#{passengerId}, #{flightId}, #{seatId},
         #{paymentId}, #{discountId})
    </insert>

</mapper>